# Lithium Browser: Update Application Support directory
# Changes from "net.imput.helium" to "com.lithium.browser"

--- a/chrome/common/chrome_paths_mac.mm
+++ b/chrome/common/chrome_paths_mac.mm
@@ -32,7 +32,7 @@ char* ProductDirNameForBundle(NSBundle*
     product_dir_name = [product_dir_name_ns fileSystemRepresentation];
 
     if (!product_dir_name) {
-      product_dir_name = "net.imput.helium";
+      product_dir_name = "com.lithium.browser";
     }
 
     // Leaked, but the only caller initializes a static with this result, so it
